# 常設ボード 第4回会議録 — 2026-09-07（告知前 4 週間の「入れる・削る」）

出席（レポート提出）: monetization-lead（`2026-09-07-monetization.md`）／ taxonomy-editor（`2026-09-07-taxonomy.md`、`data/2026-09-07-record-gate.proposed.js`、`wireframe/data/people-authority.json`）／ content-strategist（`2026-09-07-content.md`）／ design-director（`2026-09-07-design.md`）。統括: product-owner。
オーナー指示: 「Stripe の審査が通ったら告知する。それまでに、いいアイデアを入れたり、削ったりしたい。私に聞かずに会議をしてほしい」→ 確認質問なし。すべて既定で決め、オーナー依頼は本人にしかできない 6 件（§5）に限る。
会議中に実施済み: design D1（記録の信頼度・出典が `<article>` の外に出る描画バグ、`pages.js` の `html +=` 5 箇所）は修正して本番反映（commit 09-07 "Fix: trust, verification and sources render inside their record again"）。T20（AI 再調査リクエスト）は実装済みを確認し完了扱い。

判断の物差し（3 名が一致）: 告知直後の来訪者は X / Instagram からスマホで品種ページに直着地し、(1) 10 秒で「図鑑かブログか」を判定 → (2) 自分の株の名前をカタカナで検索 → (3) あれば共有、なければ離脱。効く順は **検索0件を出さない > 共有される画像 > 着地ページの純度 > 投稿導線 > 会員導線**。売上の入口（ラベル印刷・会員）は告知の受け皿として告知前に置くが、初見の 1 画面を汚さない範囲に限る。

---

## 1. 決定事項 — 衝突 9 点の裁定

| # | 論点 | 裁定 | 理由 |
|---|---|---|---|
| 1 | **採集者の扱い** | **IPNI 型データ（collectorTeam）への一致をルールにし、前回の「[要確認]→NULL」を上書き**。D2 の 5 件（dressleri T. B. Croat／papillilaminum R. L. Dressler／luxurians T. B. Croat／debile T. B. Croat & Watt／nutibarense J. M. MacDougal, D. Restrepo & D. S. Sylva）を記入、D3/D4 の 5 件（carlablackiae 'Carla Black' 2009／clarinervium Matuda 1949／regale Wallis／moronense 1967／kunayalense Vannini 2007 → Croat & Vannini）を NULL または IPNI 値に。regale の Wallis は notes「導入株は Wallis 採集とされる（原記載未確認）」。採集年は原記載で確認できた場合のみ。D5 wendlingeri の type_locality は Costa Rica、D8 crystallinum・veitchii の「Chocó」は IPNI に無いので **NULL**（分布 Colombia は維持、notes に「二次資料では Chocó」）。D1 nutibarense は 2008 Novon + 2005 Aroideana 先行発表の注記。D6 sagittatum は basionym 分岐、D7 原綴 cristallinum は aliases + 本文末注記、D9 forgetii・regale の第 2 記録は削除、D10〜D14 は提案どおり | 前回の NULL は「原記載が未確認」という理由だった。今回 IPNI API で 18/18 の型データを実照合したので根拠が変わった。「IPNI にあるのに空欄」は欠落、「無いのに記入」は捏造で、どちらも由来の正確性（判断基準②）を損なう。crystallinum の告知文 1 投稿目は「タイプ産地はチョコ」を「原産はコロンビア（POWO）」に改める |
| 2 | **ゲート修正 G1〜G9** | **全部採用**（`proposed.js` をそのまま `wireframe/js/record-gate.js` に）。G2 により AI 下書きだけの行は「記録なし」。'Dark Mama'(5) は AI 本文（Llama 3.3・出典 0）を削除して「記録なし · 作出者と交配式の出典を募集」、`formula_status='disputed'` を追加して表示は「係争中」（P5 両論併記は T31 のまま告知後）。i18n `gate_missing_publication` `gate_missing_size` | 適用後 28/31、forgetii が size で落ちるのは第 2 記録の 30cm 文が原因で、D9 の削除で戻る（→ 29/31。残りは 'Dark Mama' 記録なし・55 実生の播種日待ち）。厳しすぎる点は無く、緩さ（未記載種 40 字・Clone 2 事実）は前回決定どおり維持。M2 は「検索に出る」ことなので 'Dark Mama' が記録なしでも満たす |
| 3 | **共有画像** | **design の「標本カード」仕様を採用し、content の 2 条件を上書き適用**: 全品種を同じ台紙（1200×630、二重罫、Cormorant Garamond Bold・BIZ UDMincho・IBM Plex Mono を `fonts/og/` に自己ホスト）に組む／写真は 4:5 の台紙に `meet`／写真なしはモノグラム「A」+ `PLATE NOT YET RECORDED`／`images/anthurium.png` は出所未確認のため使わない／**「AI」「信頼度 %」はカードに載せない**（右下の状態行は「記録 n · 出典 IPNI / POWO」または「検証済」、未収録は「記録 未収録」）／中央の要約 1 行は人の記録か検証済のときだけ（AI 下書きは載せない）／**トップ15 を先に生成**。静的 PNG `images/og/<slug>.png`、`share/index.ts` fallback と静的スタブを同 URL に。**第 2 週（9/19）に前倒し**し、ソフトローンチ前に本番 | X のタイムラインで「図鑑の 1 頁」に見えることが目的で、% は初見に意味が無く「AI」は定義文と矛盾する。台紙 1 種に統一すれば写真なし 9 件も欠けに見えない。ソフトローンチの DM に品種 URL を貼るので、この時点でカードが要る |
| 4 | **棚とラベル印刷** | **T28 を分割**: **T28a 標本ラベル印刷は告知前（第 3 週 9/26）**、**T28b 棚は本告知 +2 週以降**（`share_click`・`label_print` の実測を見て 10 月上旬ボードで着手判断）。E9 は「ラベル印刷が本番」に読み替え。撤退判定（保存者 <10 かつ会員転換 0）は 12 月レビューへ | ラベル印刷は会員特典②の実体で第 2 のペイウォール（非会員が他品種を刷る瞬間）を作る。棚が無くても詳細ページの「ラベルを印刷」でその瞬間は起きる。棚は再訪者の装置で初見の 1 画面に効かず、操作行 1 語 + フッター 1 項目を足す（design・content 2:1）。monetization の +750 円/月は「来訪→ログイン +2pt」の仮説で、印刷単体でも検証できる |
| 5 | **⑤ 完了画面** | **別画面は作らない**。登録直後の品種ページの標本ラベル直下に 1 回だけ hairline 帯: mono「収録しました · この台紙を共有」+ 共有文 + 「コピー · X」。sessionStorage で再表示なし。GA `share_click{channel:'copy', source:'submit'}`。共有文は共通式 `{品種名} — {記載者 or 作出者} {年}、{タイプ産地 or 交配式}｜Aroid Origins {URL}`（cite が無ければ定義文） | 3 者一致。M7 |
| 6 | **削除の統合** | ①詳細ページの道具棚は T34 まで CSS で非表示 ②使い方ガイドは 5 見出し（区分 4 つと名前の書き方／記録の読み方 8 行／投稿: 原種・Hybrid・Clone・個体・実生 5 件無料・非公開・再調査リクエスト／ラベル印刷／会員は価格を書かず料金ページへ）、手順リスト 5 本と「コミュニティで収集・共有するプラットフォーム」「ユーザー名」削除 ③Trial 残骸（`isTrial`・バッジ・「無料お試し期間」）と死にキー `add_origin_title.en` は第 1 週に本番、料金ページの旧名称「My Seedlings サブスクリプション」は T30 まで触らない ④標題直下のバッジ行（区分・属・登録日）削除 → 上の mono 行と末尾「収録メモ」 ⑤重複スタブ `aff.besseae`・`debile(debilis)`・`mr-chandra`・`george-wagner` は生成時にディレクトリを掃除 + alias 名寄せ ⑥paywall 2 ボタン → 年額既定ラジオ + 1 ボタン（T30 ブランチ） ⑦'Dark Mama' AI 本文削除（#2） ⑧h2「画像」+ 方針 2 行 + glass カード削除、空状態は `.sheet--plate` ⑨個体区画は 1 件以上のときだけ描画 ⑩黒い `.btn--primary`「+ 由来を追加」→ mono 1 行「この台紙に書き足す — 記録を追加 · 写真を追加 · 訂正を報告」、既定フォームは本文 + 出典 URL + 送信の 3 要素、構造化欄は `<details>` | すべて「着地した 1 画面の純度」か「定義文との矛盾」。審査中に法務ページを触らない原則は維持（③⑥） |
| 7 | **content A〜E** | **A 採用（第 1 週）**: カタカナ別名をトップ15 + 収録済み全件の `aliases` に投入、検索は `pages.js:1397/1675` に `_aliases` 一致を足す（T25 の `/names/` を待たない）、静的スタブの JSON-LD `alternateName` と description 末尾「別名: …」。M2 を「学名 + カタカナの両方で 0 件を返さない」に強化。**B 採用（第 2 週）**: 検索 0 件シートに「この名前の収録を依頼」（ログイン不要、`genus_requests` に `kind='name'` 列を足して同 RPC・同ダイアログ、admin に集計）+ GA `search_zero{term}`。**C 採用（第 3 週）**: `/timeline/` 独立 URL + 年表画像 1 枚（標本カードの描画関数）。**D 一部**: 収録メモ「収録日 · 記録 n 件 · 最終更新」は第 1 週の読み順に含める。「訂正 n 件」「新着記録に訂正種別」「訂正者 by-line」は本告知 +1 週（T37 内、実際の指摘が出てから形を決める）。**E 採用（第 1 週）**: 未収録・記録なしの description は定型「記録なし · 作出者と交配式の出典を募集」 | A は「検索 0 件を出さない」の最短手段でコスト 1.5h。B は離脱を需要ログに変える唯一の装置。C は告知素材（Instagram 2 枚目）。D は指摘が 0 件の段階で仕組みを作っても検証できない。E は noindex でも共有時に露出する |
| 8 | **monetization** | **`granted` 無料付与を告知戦術として採用**（ソフトローンチ協力 2 名・本告知後の他者投稿 最初の 5 人・写真提供者、3 か月。規約 9 条に「運営者が無償で会員資格を付与することがある」は T30 ブランチ）。**ブランチ `t30-member-plan` を第 1 週に作る**（本番に出さない。500/5,000・特典 4 つ・年額既定 1 ボタン・`showPaywallModal(source)`・About 会員一覧・静的スタブ再生成。判定日は merge 30 分）。**GA**: `paywall_view{source}` `login{source}` `contribute_start{source}` `share_click copy/submit` は第 1 週、`label_print{count,member}` は T28a、`shelf_add` は T28b、`member_interest` は 10/24 決済なし告知のときだけ。M6 に `contribute_start` と `paywall_view.source` を追加。**開業届は 9/26 までに e-Tax で提出を既定**（オーナー 30 分） | 付与は開発ゼロで「会員 0 人のページ」を避ける。ブランチは審査中に本番を触らない原則と判定日の即応を両立する。開業届は PAY.JP 申請の必須書類で、Stripe 承認でも損をしない。オーナーの「品質を先に」は順番の話で、品質タスクは Claude Code が並行しており競合しない |
| 9 | **用語集 8 語** | **採用（第 2 週）**: 個体・系統・F1・オリジナル個体・管理番号・未収録・記載者・タイプ産地を用語集ページに追加（E5）。未収録シートと個体欄からリンク。basionym 著者（Sims）は索引に出さない（既定） | 区分ルールを読者が検証できる語彙。ガイド短縮と同じセッションで済む |

**採用した design の読み順（T27 前半、第 1 週）**: mono 標題行「ANTHURIUM · 原種 · NO. 013」→ h1 → 標本ラベル → 操作行（共有 · 写真を追加 · 記録を追加 · 再調査を依頼 · 編集(所有者) · 報告）→ 図版（h2 なし、写真下 mono 1 行）→ 未収録シート → 記録 → 「この台紙に書き足す」→ 個体（1 件以上）→ 関連 + 前後 + 出口 1 行「Anthurium の台帳 → · 記載史年表 → · 同じ産地 →」→ 収録メモ。プレビュー 4 パターン → 48h 無反応で本番。

---

## 2. 4 週間の実装順（Claude Code。1 日 2〜3 中規模、週 ≈20〜25h。**ソフトローンチ 9/21 までに本番に出すもの = 第 1・2 週の全部**）

| 週 | ID | タスク | h | ゲート |
|---|---|---|---|---|
| **第 1 週 9/8〜9/14** | T39-1 | ゲート G1〜G9 適用（`record-gate.js` ← proposed.js）+ i18n 2 キー + sitemap・admin 再判定 | 2 | M1・M3 |
| | T39-2 | データ修正 SQL（D1〜D5・D7・D8 NULL・D10〜D14、forgetii・regale 第 2 記録削除、'Dark Mama' AI 記録削除 + `formula_status='disputed'`）。バックアップは `docs/board/data/` | 4 | M3（C2・C4・C11） |
| | T39-3 | 由来文再生成 12 件（1, 3, 9, 31, 32, 33, 34, 38, 57, 77, 82, 83）+ 生成器に basionym 分岐・原綴注記 | 2 | M3（C5） |
| | T39-4 | 人物典拠表の組み込み（`archive.js` + `scripts/lib/people.js`、alias_of 畳み込み、kind ラベル）+ 静的 people/anthurium ディレクトリの掃除と名寄せ（D15・重複スタブ） | 2.5 | M3（C13） |
| | T27a | 読み順 A/C/D/G/H/J + 道具棚非表示 + D5 文言 + ⑧ `display:none` 既定・「報告」 + ⑥ mono 1 行 + 3 要素フォーム → プレビュー 4 パターン（9/12）→ 48h → 本番 | 7 | 着地純度 |
| | T40 | カタカナ別名 SQL（トップ15 + 収録済み全件）+ 検索に `_aliases` 一致 + JSON-LD `alternateName` + 未収録 description 定型 + Trial 残骸・死にキー削除 + GA 4 本（`paywall_view{source}` `login{source}` `contribute_start` `share_click copy`） | 3.5 | M2・M6 |
| | T30 準備 | ブランチ `t30-member-plan`（本番に出さない） | 3 | M9・M10 |
| **第 2 週 9/15〜9/21** | T24 | 原種 9 件を管理者で一括登録（GBIF 経路、waterburyanum は DOUBTFUL で保留可）+ 検収 | 3 | M1 |
| | T26 | 検証ワークフロー（列 3 本・RPC・投票固定・ラベル「検証状況」行・admin キュー）→ IPNI 一致の記載種を検証済に | 5 | M4 |
| | T29a | 標本カード生成器（§1-3 仕様、フォント 3 種コミット、deploy.yml、share fallback・スタブ URL 統一、Validator 3 枚: warocqueanum・aff. besseae・'Galaxy' → オーナーへ PNG 3 枚） | 5 | M5 |
| | T29b | ⑤ 完了帯 + 共有文の共通式 | 1.5 | M7 |
| | T41 | 検索 0 件 → 「収録を依頼」（`genus_requests.kind`）+ GA `search_zero{term}` + 0 件シート「別名で探す」1 行 | 1.5 | 告知の受け皿 |
| | T42 | 用語集 8 語 + 使い方ガイド 5 見出し化（`.card` → hairline `<section>`） | 2.5 | E5・信頼 |
| | — | オーナーのシード投稿 5 件の検収（区分・交配式・出典）、'Michelle' が無ければ告知文 1 投稿目を crystallinum に確定 | 1 | M2 |
| **第 3 週 9/22〜9/28**（ソフトローンチ中） | T25 | `/names/` + P4（旧綴り URL 解決・台帳 `= debilis` 行）+ 検索対象に人物・産地 + T38 書誌欄 + 静的スタブ・sitemap | 6 | M2（'Black Velvet'） |
| | T28a | 標本ラベル印刷（`print.css`・`buildLabelHtml`・dialog 1/2/4/8・`label--stake`・GA `label_print`・ログイン全員に無料）→ iPhone Safari 印刷確認（9/26） | 4 | E9・M6 |
| | T27b | 画像 by-line（`credit`, `user_id`）+ 個体区分 `specimen_status` + 「報告」理由選択 → `image_reports` | 4 | E8 |
| | T29c | 今日の一葉 OGP + シェア文コピー + 実生 OGP に交配式 + 人物索引「日本のブリーダー」 | 3 | M5・Instagram |
| | T43 | `/timeline/` 独立 URL + 年表画像 1 枚（標本カード描画関数）+ 静的スタブ・sitemap | 2 | 告知素材 |
| | — | ソフトローンチの指摘修正（予備） | 3 | — |
| **第 4 週 9/29〜10/3** | T24 | 第 3 週 8 件（'Dark Phoenix' 'Fat Mama' 'Wonderboy' 'Silver Blush' sp. "BVIT" sp. "Fort Sherman" sp. "villenaorum" portillae）+ antolakii aliases 確認 | 3 | M1 ≥45 |
| | ④ | 原種・Clone・個体の操作行に「この株から実生を記録 →」（`data-prefill` 流用、GA `contribute_start{source:'from_parent'}`） | 1 | 実生入口 |
| | T37 | 告知文の数値差し替え、M1〜M10 チェックスクリプト（19:00 用）、OGP 3 ツール確認手順 | 2 | 全ゲート |
| | T30 | 判定日: merge + キャッシュバスター（承認）／ PAY.JP 分岐（判定なし・不承認、サイト側 1h）／ 10/24 決済なしなら「開始したら知らせる」+ `member_interest`（1h） | 0.5〜1 | M10 |
| | 予備 | 写真到着時の版画 CSS 試作（T35）、ソフトローンチ指摘 | 3 | — |

告知後（T37 内）: D 訂正の可視化（新着記録「訂正」種別・訂正者 by-line）、T28b 棚（+2 週、実測後）、T31 P5 両論併記（10/5）、P1・P3、T32 Monstera。

---

## 3. 却下・保留した提言

| 提言 | 判断 | 理由 |
|---|---|---|
| 棚を T28 と同時に告知前（monetization 最優先） | 分割・告知後へ | 初見の 1 画面に効かず、第 2 のペイウォールは印刷単体で成立する |
| ⑥ は作業なし（monetization） | 却下 | フォームは畳まれているが黒い塗りボタンが視覚の頂点に残る（design・content） |
| 共有カードに「信頼度 n%」（design 状態行） | 却下 | 初見に意味が無く「AI」は定義文と矛盾（content）。出典名のみ |
| crystallinum・veitchii の Chocó を [要確認] 表示で残す（taxonomy の一案） | NULL に | 「不明は NULL」の原則。分布 Colombia が残る |
| 訂正 n 件・訂正者 by-line をいま実装（content D） | 告知後 +1 週 | 指摘 0 件の段階では形が決められない。更新日は第 1 週で出す |
| `/timeline/` を第 2 週に（content 案 3 週目と一致のため確認のみ） | 第 3 週 | ソフトローンチには不要、本告知素材 |
| 曖昧検索・ローマ字・和名 `japanese_name` 検索 | 見送り | Phase B（10 月）。カタカナは `aliases` で先に受ける |
| 投稿フォームのウィザード化、会員限定の早期閲覧・高解像度写真、会員バッジ色分け、LINE 共有復活 | 却下 | 既決の「やらない」、閲覧無料の約束、90 日ルール |
| 用語集 8 語を「使い方」に置く（taxonomy） | 用語集ページへ | E5（≥30 語）の分母に入れ、自動リンクが効く |
| basionym 著者（Sims）を索引化 | 出さない（既定） | 組み替えの記載者は G.Don。索引の水増しになる |

変更しないもの: 料金 500/5,000・1 プラン、閲覧無料、AI 調査の無料・承認制、ローンチ日程（ソフト 9/21、本告知 目標 10/3・上限 10/24）、決済分岐（Stripe → 10/3 判定なしで PAY.JP）、Anthurium のみ公開、価格据え置き 2027-02。

---

## 4. BOARD.md への差分（本会議で適用済み。置換前 → 置換後の要点）

- **ヘッダ**: 最終更新 → 2026-09-07（第4回）。会議録参照に `2026-09-07-*.md` を追加。
- **§3 正確性ルール②**: 「採集者・採集年は原記載またはタイプ標本引用にあるときだけ」→「**採集者は IPNI 型データ（collectorTeam）に一致させる。IPNI にあるのに空欄・IPNI に無いのに記入のどちらも不可。採集年は原記載で確認できた場合のみ**。タイプ産地は IPNI/原記載に無ければ NULL」。
- **§3 品質ゲート**: 追記「個体判定は tag `individual` のみ／AI 下書きのみは記録なし／原種の出典は命名典拠ドメイン（ipni・powo・gbif・tropicos・BHL）のみ／サイズ文は機械検査で不合格／係争中の親（`formula_status='disputed'`）は事実に数えない／出版物必須」。
- **§3 表記**: 追記「組み替え名は『G.Don が Pothos sagittatus Sims を Anthurium に組み替えた』（`basionym`）」「原綴が異なる場合は aliases + 本文末注記（debilis / cristallinum）」「カタカナ別名は `aliases`」。
- **§3 データ追加修正**: 「09-07 第4回（T39）」として D1〜D15 と削除 3 件を追加。
- **§4**: 追加「カタカナ別名」「検索 0 件 → 収録依頼」「`/timeline/`」「訂正の可視化は告知後」「未収録 description 定型」「ガイド 5 見出し」「用語集 8 語」「`granted` 無料付与の運用」。
- **§4b**: M2 → 学名 + カタカナ／ M6 に `contribute_start` `paywall_view.source`／ E9 → 「ラベル印刷が本番（棚は告知後）」／ 既定回答「'Michelle' 未収録なら 1 投稿目は crystallinum」。
- **§5**: 追加「標本カード仕様（統合版）」「読み順の確定（T27a）」「⑤ 完了帯」「⑥ mono 1 行 + 3 要素」「詳細ページ道具棚は T34 まで非表示」「棚は告知後」。
- **§6**: T20 完了／ T23 ⑦ → T39-4／ T25 に カタカナ・0 件シート／ T27 → T27a（9/12）+ T27b（9/26）／ T28 → T28a 印刷（9/26）+ T28b 棚（告知 +2 週）／ T29 → 9/19 前倒し + 標本カード + ⑤／ T30 に `t30-member-plan` ブランチ／ T37 に D と判定日手順／ 新規 T39（taxonomy 一括）・T40（content 第 1 週）・T41（0 件依頼）・T42（用語集 + ガイド）・T43（`/timeline/`）。
- **§7**: #6 に判定日手順（Price 2 本 → Portal → Webhook → Secrets → 実決済 500 円 → 解約 → 返金）、#7 に `anthurium.png` 既定「使わない」、#8 にプレビュー 3 回（読み順 9/12・標本カード PNG 9/19・印刷 9/26）、**#9 開業届 9/26**、**#10 無料付与の相手**。
- **§9**: 第4回を追加。

## 5. オーナーにしかできない作業（6 件、各に既定。無回答なら既定で進む）

| # | 依頼 | 手順・所要 | 期限 | 既定 |
|---|---|---|---|---|
| 1 | **Stripe の確認** | ダッシュボードを 9/8・9/19・9/26 に見る。「審査中」のまま 2〜3 日変化なしなら `docs/stripe-application.md` §5 の文面でサポートへ。承認なら Product「Aroid Origins 会員」に Price 2 本（500 円/月、5,000 円/年、税込・トライアルなし）→ Customer Portal 有効化 → Webhook 登録 → 画面共有で 4 つの Secrets を Supabase へ → merge 後に**本人が月額 500 円で実決済**（M10）→ Portal から解約 → Stripe で返金（手数料 ≈18 円は戻らない）。各 30 分 | 9/8〜10/4 | 10/3 判定なしなら T30 先行 + PAY.JP 申請。10/24 未稼働なら決済なしで告知 |
| 2 | **開業届（e-Tax）** | マイナンバーカードで e-Tax → 「個人事業の開業届出書」。屋号は任意（Aroid Origins 可）、事業内容「ウェブサイトによる情報提供」。控え = 受信通知 PDF を保存。30 分・費用 0 | 9/26 | 未提出なら PAY.JP 申請ができず、10/3 判定なし時は自動的に「10/24 決済なし告知」の分岐に入る |
| 3 | **無料付与の相手選び** | ソフトローンチ協力のブリーダー 2 名（§7-3 と同一人物で可）を決め、9/21 の DM と同時に管理画面「無料付与」で 3 か月。本告知後は他者投稿の最初の 5 人と写真提供者に同じ操作。各 10 分 | 9/21 | 相手がいなければ付与なしで告知（About の会員一覧は非表示のまま） |
| 4 | **写真** | clarinervium・regale 最優先、次に luxurians・moronense・nutibarense・sagittatum・交配種 2〜3（葉 1 枚正面・無地背景・長辺 2000px 以上）+ 棚の引き 1 枚。1.5h | 9/21 | 「図版未収録」のまま告知し、告知文に「写真はまだ半分」。`images/anthurium.png` は出所未確認として使わない |
| 5 | **iPhone 実機のプレビュー確認 3 回** | ①読み順（品種ページ 4 パターン）9/12 ②標本カード PNG 3 枚を X の投稿プレビューと LINE で 9/19 ③ラベル印刷（共有シート → プリント）9/26。「OK」か気になる点の箇条書き、各 15 分 | 各日 | 48 時間無反応なら本番反映 |
| 6 | **シード投稿 5 件と実生の播種日** | 'Dorayaki' → 'Red Vein Dark Phoenix' → 'Michelle' → 'Zara' → 'Titanium'（1.5h、P2 済みなので「複合交配」チェックが使える）、forgetii × 'Titanium' の播種日・作出者、@hare_anthurium の帰属 1 行 | 9/12 | 無ければ M2 が欠け本告知は翌土曜へ。告知文 1 投稿目は crystallinum。播種日不明は未収録のまま。帰属は本人・栽培家名 |

質問しないもの（既定で進む）: 前回 §2-6 の全項目、採集者の IPNI 準拠、Chocó の NULL、'Dark Mama' の記録なし化、共有カードの書体、棚の延期、用語集の置き場所、GA イベント名。
